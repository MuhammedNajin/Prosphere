export enum TeamRole {
    HR = "HR",
    Owner = "Owner",
    Employee = "Employee",
  }
  
  export enum CompanyStatus {
    Pending = "pending",
    Uploaded = "uploaded",
    Rejected = "rejected",
    Verified = "verified",
  }