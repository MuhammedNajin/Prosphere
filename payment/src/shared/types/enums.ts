export enum PlanType {
    BASIC = 'basic',
    PREMIUM = 'premium'
  }
  
  export enum SubscriptionStatus {
    ACTIVE = 'ACTIVE',
    CANCELLED = 'CANCELLED',
    EXPIRED = 'EXPIRED',
    SUSPENDED = 'SUSPENDED',
    TRIAL = 'TRIAL'
  }
  
  export enum PaymentStatus {
    PENDING = 'pending',
    SUCCESS = 'success',
    FAILED = 'failed'
  }